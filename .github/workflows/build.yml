name: Build and Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Set up OCaml
        uses: ocaml/setup-ocaml@v2
        with:
          ocaml-compiler: 5.2.x
          dune-cache: true
          opam-pin: false
          opam-depext: false

      - name: Install dependencies
        run: |
          opam install -y rocq-core.9.0.0
          opam install -y rocq-stdlib.9.0.0
          opam install -y coq-paco.4.2.3
          opam install -y coq-itree.5.2.0
          opam install -y coq-ext-lib.0.13.0

      - name: Build project
        run: |
          eval $(opam env)
          dune build

      - name: Check admitted proofs
        run: |
          eval $(opam env)
          ./tools/check_admitted.sh --max 15 --fail
